<?xml version="1.0" encoding="utf-8" ?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingTop="14dp"
        android:paddingEnd="14dp"
        android:paddingBottom="8dp"
        android:paddingStart="14dp"
        android:gravity="top"
    >


        <TextView
            android:id="@+id/widget_quote"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginEnd="6dp"
            android:text="Loading quote..."
            android:textSize="14sp"
            android:textColor="@android:color/black"
            android:gravity="top|start"
            android:includeFontPadding="false"
        />

        <TextView
            android:id="@+id/widget_author"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="12sp"
            android:textStyle="italic"
            android:textColor="@android:color/darker_gray"
            android:gravity="end"
        />

    </LinearLayout>

    <TextView
        android:id="@+id/widget_sync_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="-2dp"
        android:layout_marginEnd="6dp"
        android:contentDescription="Sync widget time"
        android:padding="0dp"
        android:text="↻"
        android:textColor="@android:color/black"
        android:textSize="18sp"
    />

    <!-- Page up (visible only when quote spans multiple pages) -->
    <TextView
        android:id="@+id/widget_page_up"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="28dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="Page up"
        android:padding="0dp"
        android:text="▲"
        android:textColor="@android:color/black"
        android:textSize="14sp"
        android:visibility="gone"
    />

    <!-- Page down (visible only when quote spans multiple pages) -->
    <TextView
        android:id="@+id/widget_page_down"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="32dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="Page down"
        android:padding="0dp"
        android:text="▼"
        android:textColor="@android:color/black"
        android:textSize="14sp"
        android:visibility="gone"
    />

</FrameLayout>
